# Matrix Operations

Use `size(A)` to inspect dimensions, `A.'` for a non-conjugating transpose, `A'` for conjugate transpose, `A\\b` to solve `A*x=b`, and `eig(A)` for eigenvalues. Prefer solving with `\\` instead of forming `inv(A)`.
