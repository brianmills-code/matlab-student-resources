# Plotting Guide

A clear figure normally includes `figure`, `plot`, `grid on`, descriptive `xlabel` and `ylabel`, a useful `title`, and `legend` when multiple series are shown. Use `exportgraphics` for reproducible output.
